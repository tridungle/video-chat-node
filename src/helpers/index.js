import {
  getSocketById,
  getSocketByUserId,
  getContactIdBySocketIds,
  getUserBySocketId
} from '@components/users/user.services'
export default { getSocketById, getSocketByUserId, getContactIdBySocketIds, getUserBySocketId }
